jupyterlab==4.3.5
pandas==2.2.3

# Interactive Spark Connect client. pyarrow is pinned to the version used by the
# Spark 4.1.3 runtime image so notebook queries and Arrow-backed collection use
# one Arrow version end to end; grpcio pins keep prebuilt wheels available.
pyspark[connect]==4.1.3
grpcio==1.76.0
grpcio-status==1.76.0
protobuf==6.31.1
pyarrow==18.1.0
zstandard==0.25.0
