# apache-airflow is pinned to the base image version so provider packages can
# never resolve a newer Airflow into the image or the local virtualenv.
apache-airflow==2.10.5
requests==2.32.3
pyyaml==6.0.2
pyspark==4.1.3
pandas==2.2.3
pyarrow==18.1.0
zstandard==0.25.0
apache-airflow-providers-apache-kafka==1.4.1
confluent-kafka==2.6.1
